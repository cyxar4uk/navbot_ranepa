# 🚀 Быстрая настройка автоматического деплоя

## Самый простой способ (GitHub Actions)

**✅ Безопасно:** Все действия выполняются ТОЛЬКО в папке `/www/wwwroot/testingsmth.anyway-community.ru/navbot_ranepa`. Другие сайты не затрагиваются.

### 1. На сервере (через aaPanel Terminal или SSH):

```bash
# Создайте SSH ключ для GitHub Actions (можно в любой директории)
ssh-keygen -t ed25519 -C "github-actions-navbot" -f ~/.ssh/github_actions_navbot -N "

# Добавьте публичный ключ в authorized_keys
cat ~/.ssh/github_actions_navbot.pub >> ~/.ssh/authorized_keys

# Покажите приватный ключ (скопируйте его полностью)
cat ~/.ssh/github_actions_navbot
```

### 2. В GitHub репозитории:

1. Перейдите: **Settings → Secrets and variables → Actions**
2. Нажмите **New repository secret**
3. Добавьте секреты:

| Имя | Значение | Пример |
|-----|----------|--------|
| `SERVER_HOST` | IP или домен сервера | `208.92.227.20` |
| `SERVER_USER` | SSH пользователь | `root` |
| `SSH_PRIVATE_KEY` | Приватный ключ (весь файл `~/.ssh/github_actions_navbot`) | `-----BEGIN OPENSSH PRIVATE KEY-----...` |
| `SSH_PORT` | Порт SSH (опционально) | `22` |

### 3. Очистка сервера (если есть локальные изменения)

**⚠️ Важно:** Если на сервере есть локальные изменения, которые конфликтуют с репозиторием, выполните на сервере:

```bash
cd /www/wwwroot/testingsmth.anyway-community.ru/navbot_ranepa
git fetch origin
git reset --hard origin/main  # или origin/master если используете master
git clean -fd
```

Это синхронизирует сервер с репозиторием и удалит локальные изменения.

### 4. Готово! 

Теперь при каждом `git push` изменения автоматически деплоятся на сервер.

---

## Альтернатива: Ручной деплой скрипт

Если не хотите настраивать GitHub Actions, используйте скрипт:

```bash
# На сервере сделайте скрипт исполняемым
chmod +x /www/wwwroot/testingsmth.anyway-community.ru/navbot_ranepa/deploy.sh

# Запустите деплой
cd /www/wwwroot/testingsmth.anyway-community.ru/navbot_ranepa
./deploy.sh
```

Или просто вручную:
```bash
cd /www/wwwroot/testingsmth.anyway-community.ru/navbot_ranepa
git pull
docker-compose up -d --build
```

---

## Проверка работы

После настройки GitHub Actions:

1. Сделайте любой коммит и push:
```bash
git add .
git commit -m "Test deployment"
git push
```

2. Проверьте статус в GitHub: **Actions** вкладка в репозитории

3. На сервере проверьте логи:
```bash
docker-compose logs -f backend
```
