# Стратегия деплоя: баланс между скоростью и надежностью

## Проблема
Полная пересборка всех Docker образов с `--no-cache` занимает очень много времени (5-10+ минут), но необходима для избежания проблем с "фантомными" кэшированными образами.

## Решение: три уровня деплоя

### 1. 🚀 `deploy-fast.sh` - Быстрый деплой (рекомендуется)
**Когда использовать:** Обычные обновления кода, когда нет проблем с Docker образами.

**Что делает:**
- Использует Docker layer cache
- Пересобирает только измененные сервисы (Docker сам определяет)
- Очищает только dangling images
- **Время:** 1-3 минуты

**Использование:**
```bash
./deploy-fast.sh              # Пересобрать все измененные сервисы
./deploy-fast.sh backend      # Пересобрать только backend
./deploy-fast.sh frontend     # Пересобрать только frontend
```

### 2. 🧠 `deploy-smart.sh` - Умный деплой (оптимально)
**Когда использовать:** Автоматический деплой, когда нужно минимизировать время сборки.

**Что делает:**
- Анализирует git изменения между коммитами
- Пересобирает только сервисы с изменениями в коде
- Если изменений нет - только перезапуск контейнеров
- Сохраняет маркер последнего деплоя в `.last_deploy_commit`
- **Время:** 30 секунд - 2 минуты (зависит от изменений)

**Использование:**
```bash
./deploy-smart.sh
```

### 3. 🧹 `deploy-clean.sh` - Полная очистка (только при проблемах!)
**Когда использовать:** 
- При проблемах с "фантомными" образами
- После изменения базовых образов (Python, Node версии)
- При подозрении на проблемы с кэшем
- **НЕ используйте для обычных обновлений!**

**Что делает:**
- Полная очистка всех образов проекта
- Очистка build cache
- Пересборка БЕЗ кэша (`--no-cache`)
- **Время:** 5-10+ минут

**Использование:**
```bash
./deploy-clean.sh  # Запросит подтверждение
```

## GitHub Actions Workflow

Обновлен для использования кэша:
- Удаляет только остановленные контейнеры
- Сохраняет образы для кэша
- Использует `docker-compose build` без `--no-cache`
- Очищает только dangling images

## Рекомендации

### Для обычных обновлений:
```bash
./deploy-fast.sh
```

### Для автоматического деплоя (CI/CD):
```bash
./deploy-smart.sh
```

### При проблемах:
```bash
./deploy-clean.sh
```

## Как Docker определяет изменения?

Docker использует layer caching:
1. Проверяет каждый слой Dockerfile
2. Если файлы не изменились - использует кэш
3. Если изменились - пересобирает с этого слоя

**Пример:**
```
Dockerfile:
1. FROM python:3.11-slim          # Кэш если образ не обновлялся
2. COPY requirements.txt          # Кэш если requirements.txt не изменился
3. RUN pip install               # Кэш если шаг 2 в кэше
4. COPY . .                      # Пересборка если код изменился
5. CMD uvicorn...                # Пересборка если шаг 4 изменился
```

## Оптимизация Dockerfile для лучшего кэширования

Убедитесь, что в Dockerfile:
1. ✅ Зависимости копируются ДО кода
2. ✅ Установка зависимостей отдельным слоем
3. ✅ Код копируется последним

**Пример хорошего порядка:**
```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .          # ← Сначала зависимости
RUN pip install -r requirements.txt
COPY . .                        # ← Потом код
CMD uvicorn...
```

## Мониторинг

После деплоя проверьте:
```bash
docker-compose ps              # Статус контейнеров
docker-compose logs backend    # Логи backend
curl http://127.0.0.1:8001/health  # Health check
```

## Troubleshooting

**Проблема:** Образы не обновляются после изменений
**Решение:** Используйте `deploy-clean.sh` один раз, затем вернитесь к `deploy-fast.sh`

**Проблема:** Ошибки при сборке из-за старого кэша
**Решение:** Используйте `deploy-clean.sh` для полной очистки

**Проблема:** Медленный деплой
**Решение:** Используйте `deploy-smart.sh` для автоматического определения изменений
