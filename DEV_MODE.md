# Режим разработки (Dev Mode)

## Обзор

Система теперь поддерживает режим разработки, который позволяет тестировать приложение в браузере без Telegram WebApp.

## Как использовать

### 1. Тестирование в браузере (без Telegram)

**Backend:**
- DEBUG режим уже включен в `docker-compose.yml` (`DEBUG: "true"`)
- При отсутствии токена авторизации создается dev-пользователь (telegram_id: 999999999)
- Dev-пользователь автоматически получает права администратора

**Frontend:**
- В режиме разработки (`npm run dev` или `import.meta.env.DEV`) автоматически используется токен "dev"
- Все API запросы работают без Telegram WebApp
- AI-ассистент доступен и работает

### 2. Специальное сообщение в боте для админ панели

**В Telegram боте:**
- Отправьте одно из сообщений: `admin`, `админ`, `админка`, `панель администратора`
- Бот ответит сообщением с инлайн кнопкой "🔐 Открыть админ панель"
- Нажмите на кнопку, чтобы открыть админ панель в WebApp

**В AI-ассистенте (в приложении):**
- Отправьте одно из сообщений: `admin`, `админ`, `админка`, `панель администратора`
- Ассистент ответит с кнопкой для перехода в админ панель

## Настройка

### Включение DEBUG режима

В `.env` файле или `docker-compose.yml`:
```env
DEBUG=true
```

### Настройка URL админ панели

В `.env` файле:
```env
TELEGRAM_WEBAPP_URL=https://testingsmth.anyway-community.ru
```

## Важно

- В production режиме (`DEBUG=false`) требуется валидный Telegram initData
- Dev-пользователь создается только в DEBUG режиме
- Для работы бота требуется настроить `TELEGRAM_BOT_TOKEN` в `.env`

## Тестирование

1. **AI-ассистент:**
   - Откройте `/assistant` в браузере
   - Отправьте сообщение - должно работать без ошибок
   - Отправьте "admin" - должна появиться кнопка для админ панели

2. **Админ панель:**
   - Откройте `/admin` в браузере
   - Войдите через форму логина
   - Все функции должны работать

3. **Telegram бот:**
   - Отправьте боту сообщение "admin"
   - Должна появиться кнопка для открытия админ панели
