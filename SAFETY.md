# 🔒 Безопасность деплоя

## Гарантии безопасности

✅ **Все действия выполняются ТОЛЬКО в директории проекта:**
```
/www/wwwroot/testingsmth.anyway-community.ru/navbot_ranepa
```

✅ **Другие сайты НЕ затрагиваются**, потому что:

1. **Явный путь в скриптах:**
   - GitHub Actions workflow использует `cd` в конкретную директорию
   - Проверка наличия `docker-compose.yml` перед выполнением команд
   - Использование `-f docker-compose.yml` для явного указания файла

2. **Docker Compose изолирован:**
   - Команды `docker-compose` выполняются с явным указанием файла
   - Контейнеры именуются с префиксом проекта (`navbot_ranepa_*`)
   - Другие проекты имеют свои собственные `docker-compose.yml` файлы

3. **Git работает только в своем репозитории:**
   - `git pull` выполняется только в директории проекта
   - Не влияет на другие git репозитории на сервере

## Что происходит при деплое:

```bash
# 1. Переход ТОЛЬКО в директорию navbot_ranepa
cd /www/wwwroot/testingsmth.anyway-community.ru/navbot_ranepa

# 2. Проверка что мы в правильном месте
if [ ! -f "docker-compose.yml" ]; then exit; fi

# 3. Обновление кода ТОЛЬКО этого репозитория
git pull

# 4. Работа ТОЛЬКО с контейнерами этого проекта
docker-compose -f docker-compose.yml down
docker-compose -f docker-compose.yml up -d --build
```

## Проверка изоляции

После настройки деплоя проверьте:

```bash
# Список контейнеров (должны быть только navbot_ranepa_*)
docker ps

# Список сетей Docker (каждый проект в своей сети)
docker network ls

# Проверка что другие сайты работают
# (зайдите на другие ваши сайты в браузере)
```

## Если что-то пошло не так

1. **Откат изменений:**
```bash
cd /www/wwwroot/testingsmth.anyway-community.ru/navbot_ranepa
git reset --hard HEAD~1
docker-compose up -d --build
```

2. **Проверка логов:**
```bash
docker-compose logs backend
docker-compose logs frontend
```

3. **Остановка деплоя:**
   - В GitHub: Settings → Actions → Disable workflow
   - Или удалите файл `.github/workflows/deploy.yml`

## Дополнительная безопасность (опционально)

Если хотите еще больше изоляции, можно:

1. **Создать отдельного пользователя для деплоя:**
```bash
useradd -m -s /bin/bash deployer
# Настроить права только на директорию проекта
```

2. **Использовать Docker Compose с префиксом проекта:**
```bash
# В docker-compose.yml уже используется имя проекта из директории
# Это автоматически изолирует контейнеры
```

3. **Ограничить SSH доступ:**
```bash
# В ~/.ssh/authorized_keys можно добавить ограничения:
command="cd /www/wwwroot/testingsmth.anyway-community.ru/navbot_ranepa && $SSH_ORIGINAL_COMMAND" ssh-ed25519 ...
```

---

**Вывод:** Настройка полностью безопасна и не затрагивает другие проекты на сервере! 🛡️
