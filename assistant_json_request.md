7) Чтобы я подогнал всё 1-в-1 под твою реализацию

Покажи мне реальный JSON, который сейчас возвращает get_all_events() (прямо копипастой сюда).
Я тогда:

- скажу, что переименовать
- какой минимум полей добавить
- и дам готовый кусок для системного промпта именно под ваши поля

Если не можешь достать JSON — скажи, где он формируется в коде (примерно файл), и мы по нему пойдём.
